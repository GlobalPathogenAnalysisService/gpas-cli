name: gpas-cli-dev
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pip
  - python=3.10
  - read-it-and-keep
  - samtools
  - pip:
    - pytest
    - black
    - pre-commit
    - "--editable=git+https://github.com/GenomePathogenAnalysisService/gpas-uploader.git#egg=gpas_uploader"
    - "--editable=git+https://github.com/GenomePathogenAnalysisService/gpas-cli.git#egg=gpas"

prefix: /Users/bede/miniconda3/envs/gpas-cli-dev
